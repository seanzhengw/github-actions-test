on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Cmake by docker action
    steps:
    - name: cmake and make
      id: hello
      uses: seanzhengw/hello-world-docker-action@v2.0.0
